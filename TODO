fix airplane mode trigger issue, at system.ts
delay time period keyboard brightness  
time period keyboard brightness trigger the action in revert (when the routine ends) when the condition meets on extension init

see how clipboard change is avoiding trigger on simple gnome shell refresh/login/ wakeup from sleep/init and only triggering on real true change. We do not want to rely on timer or any other clunk unreliable approach This trigger should only trigger ON true event change. ie. when we know the clipboard content changed from something to something.